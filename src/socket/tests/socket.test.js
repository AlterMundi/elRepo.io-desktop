const assert = require('assert');
const { socket } = require('../socket');

describe('SOCKET API', function() {

    describe('Handle connection', function() {
        it('on connection', ()=>{

        });

        it('on disconection', ()=>{

        });
    });

    describe('Handle messages', function() {
        it('write', ()=>{

        });

        it('read', ()=>{

        });
    });
});